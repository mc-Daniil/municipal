# Кризотютя | Easy | Crypto

## Информация

Котик! Я тебе на 8 марта приготовил подарок, но он как-то перемешался по дороге в пакете. Я не знаю, как его восстановить! Может, ты сможешь?
В результате шифрования приложенной программой получился следующий вывод:
ciphertext = 6653a882e12bba271dcf307092be3b2aa331e51edb777c8c8fd831a02b1bd13073cbec6514a26aa10f
hint = 6b7f87f0d33fbb6f10d90c6487af2855b436f90180
В программе флаг был убран из кода, восстановите его из зашифрованных данных.

## Выдать участинкам

[encryption.py](public/encryption.py)

## Решение

В приложенном скрипте релизован XOR-шифр (шифр Вернама/шифр Виженера с бинарным алфавитом).

Этот шифр (подобно лежащему в его основе оператору XOR) имеет симметрию: если `x ^ y = z`, то `x = y ^ z`, а `y = x ^ z`.

Для шифрования это означает, что достаточно "зашифровать" шифротекст используя открытый текст, как ключ, чтобы получить изначальный ключ. Другими словами, раз `plaintext ^ key = ciphertext`, то `ciphertext ^ plaintext = key`. Поэтому достаточно вычислить `xor_cipher("Can you decrypt this?".encode(), hint)`, чтобы получить ключ.

Подобным образом происходит и расшифровка. Все это приведено в скрипте [solve.py](solve/solve.py).

## Флаг

`NMARK{this_flag_could_be_random_e539ab41}`

